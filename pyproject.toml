[project]
name = "hyprland-monitors"
version = "0.1.0"
description = "Monitor management utilities for Hyprland"
requires-python = ">=3.12"
license = "MIT"
readme = "README.md"
keywords = ["hyprland", "monitor", "wayland", "compositor"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Desktop Environment",
    "Typing :: Typed",
]
dependencies = ["hyprland-socket>=0.3.0"]

[project.urls]
Repository = "https://github.com/BlueManCZ/hyprland-monitors"

[dependency-groups]
dev = ["pytest>=9.0", "ruff>=0.15"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
